# Wolfronix Production Environment Configuration
# Copy this file to .env and update the values

# =============================================================================
# REQUIRED - Change these before deploying!
# =============================================================================

# Database password (use a strong random password)
DB_PASSWORD=your-super-secure-database-password-here

# JWT secret for authentication (use: openssl rand -hex 32)
JWT_SECRET=your-64-character-hex-secret-here

# Master encryption key (use: openssl rand -hex 32)
MASTER_KEY=your-64-character-hex-master-key-here

# =============================================================================
# SERVER CONFIGURATION
# =============================================================================

# Your domain name (or IP address if no domain)
DOMAIN=api.wolfronix.com

# Data storage path on host machine
DATA_PATH=/opt/wolfronix/data

# =============================================================================
# OPTIONAL - Defaults are usually fine
# =============================================================================

# Environment
WOLFRONIX_ENV=production

# Memory limits (adjust based on your server)
# Your server has 32GB, we'll use 24GB for Wolfronix
GOMEMLIMIT=24GiB

# SSL mode (letsencrypt or selfsigned)
SSL_MODE=letsencrypt

# Email for Let's Encrypt notifications
SSL_EMAIL=admin@yourdomain.com
