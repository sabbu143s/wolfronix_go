# Wolfronix Production Environment Configuration
# Copy this file to .env and update the values
# Or run deploy.sh — it auto-generates secure secrets for you

# =============================================================================
# REQUIRED - Change these before deploying!
# =============================================================================

# Database password (use a strong random password)
# Generate: openssl rand -hex 16
DB_PASSWORD=your-super-secure-database-password-here

# JWT secret for authentication
# Generate: openssl rand -hex 32
JWT_SECRET=your-64-character-hex-secret-here

# Master encryption key
# Generate: openssl rand -hex 32
MASTER_KEY=your-64-character-hex-master-key-here

# Admin API key for enterprise endpoints (/api/v1/enterprise/*)
# Generate: openssl rand -hex 32
ADMIN_API_KEY=your-64-character-hex-admin-key-here

# =============================================================================
# SERVER CONFIGURATION
# =============================================================================

# Your domain name (or IP address if no domain)
DOMAIN=api.wolfronix.com

# Data storage path on host machine
DATA_PATH=/opt/wolfronix/data

# =============================================================================
# CORS
# =============================================================================

# Comma-separated allowed origins (or * for all — NOT recommended in production)
ALLOWED_ORIGINS=https://api.wolfronix.com

# =============================================================================
# ENTERPRISE CLIENT DB (optional)
# =============================================================================
# Only configure if using enterprise mode with external client databases

# CLIENT_DB_API_ENDPOINT=http://your-client-api:8080/api
# CLIENT_DB_API_KEY=your-client-api-key
# CLIENT_DB_TYPE=custom_api
# CLIENT_DB_TIMEOUT=30s
# CLIENT_DB_RETRY_COUNT=3

# =============================================================================
# OPTIONAL - Defaults are usually fine
# =============================================================================

# Environment
WOLFRONIX_ENV=production

# Memory limits (adjust based on your server RAM)
# Example: 24GiB for 32GB server, 6GiB for 8GB server
GOMEMLIMIT=24GiB

# SSL mode (letsencrypt or selfsigned)
SSL_MODE=letsencrypt

# Email for Let's Encrypt notifications
SSL_EMAIL=admin@yourdomain.com
