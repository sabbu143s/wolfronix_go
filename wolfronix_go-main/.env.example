# === Wolfronix Environment Variables ===
# Copy this file to .env and fill in your values

# Database
DB_USER=admin
DB_PASSWORD=          # REQUIRED: Set a strong password
DB_NAME=client_vault

# Admin API Key (for /admin/* and /api/v1/enterprise/register endpoints)
ADMIN_API_KEY=        # REQUIRED: Generate with `openssl rand -hex 32`

# Production only (deploy/docker-compose.prod.yml)
# DB_PASSWORD=        # Same as above
# JWT_SECRET=         # REQUIRED: `openssl rand -hex 32`
# MASTER_KEY=         # REQUIRED: `openssl rand -hex 32`

# SaaS Portal (Wolfronix_saas/backend/.env)
# JWT_SECRET=         # REQUIRED
# SESSION_SECRET=     # REQUIRED
# FRONTEND_URL=http://localhost:5500
# WOLFRONIX_ADMIN_KEY= # Must match ADMIN_API_KEY above

# Optional: Client DB API
# CLIENT_DB_API_ENDPOINT=
# CLIENT_DB_API_KEY=
# CLIENT_DB_TYPE=custom_api
# CLIENT_DB_TIMEOUT=30s
# CLIENT_DB_RETRY_COUNT=3
